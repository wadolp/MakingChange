import java.util.*;

public class Purse {
    //construction of Hash Map
    Map<Denomination,Integer> map = new HashMap<Denomination,Integer>();

    //Function to add currency to Purse
    public void add(Denomination type, int num) {

    }

    //Function to remove currency and return amount in purse.
    public double remove(Denomination type, int num) {
        double final_total = 0;
        final_total = this.getValue();
        return final_total;
    }

    //Method to return amount in purse
    public double getValue(){
        double total = 0;
        //Function to add up total amount
        //***************************************************************************
        return total;
    }
}
